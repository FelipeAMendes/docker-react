sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t felipe/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true felipe/docker-react npm run test -- --coverage
